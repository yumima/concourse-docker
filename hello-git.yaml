#resources:
#- name: hello-git
#  type: git
#  source:
#    uri: git@github.com:cisco-cis/staas-playground.git 
#    branch: master
#    private_key: {{staas-git-key}}

jobs:
- name: hello-world
  plan:
  - task: say-hello
    config:
      platform: linux
      image_resource:
        type: docker-image
        source: {repository: ubuntu}
      run:
        path: echo
        args: ["Hello, world!"]
